- [x] отдать на тест route_lozon_id
- [x] отписать потребителям batchPlaceArticle насчет лимитов
- [ ] разобраться с circut breaker
- [x] отписать по времени релиза
- [ ] какой запрос 66f8583600739a07 выполняется после нераспреда
- [ ] правки по DLQ



121
? вопрос по поводу ResourceExhausted, context cancelled и тп
? в базе запрос шутсрый, а через ручку ждем долго, странно
? если мы нераспред достаем из кафке ридера, из дкд просто обогощаем
? как подружить наш rate limit и ResourceExhausted от клиента



```json
{
   "clients": [], // for all
   "handlers": [
   "/ozon.express.supply_chain.logistics.inbound.fresh_inbound_proxy.placement.v1.PlacementServiceV1/BatchPlaceArticle"
   ],
   "limit": 60,
   "timeout": "3s", // ?? если 0, то возвращаем сразу ошибку (пока не пойму какую)
   "type": "local",
   "is_per_client": false
}
```


https://gitlab.ozon.ru/platform/scratch/-/tree/master/pkg/mw#ratelimiter

- для конкретного batch такой вариант не подойдет, лушче старое решение, но чуть его переосмыслить
- if err := c.limiter.Wait(ctx); err != nil - можем использовать Wait с ctx.WithTimeout - чтобы аккуратно обрубать, если слишком долго висим у клиента
- через Allow() - достигли лимита, отдаем ошибку
- также объеденить наш rate limit с ResourceExhausted и от клиента