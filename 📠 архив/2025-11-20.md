- [x] насчет проблемы с fresh-inbound-proxy
- [x] собрать все задачи по fresh-inbound-proxy
- [x] правки dlq 
- [x] правки по route_lozon_id с порядком
- [x] отписать container-processor-gw
- [ ] обработка resourceExhausted + circut breaker
	- [x] в SortArticles и ProcessTransit поправить лимиты с ответом в failedPlacment
	- [ ] добавить обработку resourceExhausted от клиентов
	- [ ] закончить circut breaker



- [x] flow
- [x] flow
- [x] flow
- [x] flow
- [x] flow
- [ ] flow
- [ ] flow
- [ ] flow
- [ ] flow



```
"SELECT p.id, p.posting_number, p.posting_lozon_id, p.warehouse_id, p.address_enc::BYTEA as address_enc, p.time_slot_start_at, p.time_slot_end_at, p.lat_enc::BYTEA as lat_enc, p.lng_enc::BYTEA as lng_enc, p.delivery_type, delivery_variant_id, p.courier_zone_name_enc::BYTEA as courier_zone_name_enc, p.article_box_tare_id, COUNT(p.id) OVER () AS total_points, p.delivery_name_enc, p.delivery_phone_enc, p.state, p.created_at, p.united_delivery_type, p.labels, p.scan_it FROM point AS p 

WHERE (NOT EXISTS (SELECT 1 FROM route_point_v2 rp WHERE rp.point_id = p.id) AND p.state IN ($1, $2, $3, $4, $5, $6, $7) AND p.deleted_at IS NULL) AND p.warehouse_id = $8 AND p.posting_number LIKE $9 AND p.time_slot_start_at >= $10 AND p.time_slot_end_at <= $11 AND p.time_slot_start_at >= $12 LIMIT 1500 OFFSET 0"
```

```
"SELECT p.id, p.posting_number, p.posting_lozon_id, p.warehouse_id, p.address_enc::BYTEA as address_enc, p.time_slot_start_at, p.time_slot_end_at, p.lat_enc::BYTEA as lat_enc, p.lng_enc::BYTEA as lng_enc, p.delivery_type, delivery_variant_id, p.courier_zone_name_enc::BYTEA as courier_zone_name_enc, p.article_box_tare_id, COUNT(p.id) OVER () AS total_points, p.delivery_name_enc, p.delivery_phone_enc, p.state, p.created_at, p.united_delivery_type, p.labels, p.scan_it FROM point AS p 

LEFT JOIN route_point_v2 AS rp on (rp.point_id = p.id) LEFT JOIN route AS r on (r.id = rp.route_id AND r.deleted_at IS NULL) 

WHERE ((r.route_lozon_id = $1 OR r.route_lozon_id IS NULL) AND p.deleted_at IS NULL) AND p.warehouse_id = $2 AND p.posting_number LIKE $3 AND p.time_slot_start_at >= $4 AND p.time_slot_end_at <= $5 AND p.time_slot_start_at >= $6 GROUP BY p.id HAVING COUNT(r.id) FILTER (WHERE r.id IS NOT NULL) = 0 LIMIT 1500 OFFSET 0"
```


---

```json
{
   "clients": [], // for all
   "handlers": [
   "/ozon.express.supply_chain.logistics.inbound.fresh_inbound_proxy.placement.v1.PlacementServiceV1/BatchPlaceArticle"
   ],
   "limit": 60,
   "timeout": "3s", // ?? если 0, то возвращаем сразу ошибку (пока не пойму какую)
   "type": "local",
   "is_per_client": false
}
```


https://gitlab.ozon.ru/platform/scratch/-/tree/master/pkg/mw#ratelimiter

- для конкретного batch такой вариант не подойдет, лучше старое решение, но чуть его переосмыслить
- if err := c.limiter.Wait(ctx); err != nil - можем использовать Wait с ctx.WithTimeout - чтобы аккуратно обрубать, если слишком долго висим у клиента
- через Allow() - достигли лимита, отдаем ошибку
- также объеденить наш rate limit с ResourceExhausted и от клиента


```json
{
 "o3:///container-processor-gw:grpc": {
  "disabled": false,
  "force_open": false,
  "timeout": "3s", // интервал, в который должен уложиться ответ от сервиса. Если запрос не уложился в отведенное время, в ответ прийдет ошибка `context.DeadlineExceeded`, запрос попадет в статистику как "плохой", и может привести к разрыву брейкера
  "opener": {
            "err_percent": 20, // процент ошибок за последние `10s`, который должен привести к разрыву брейкера
            "requests_min": 5 // минимум, чтобы перевести в статус Opened
          },
  "closer": {
            "sleep_window": "5s",
            "attempts": 3, // сколько раз можем сходить для проверки жизнеспособности сервиса
            "required_successful": 3 // кол-во успешных ответов, чтобы выключить брейкер
    },
    metrics_disabled: false, // метрики
 }
}
```





